import { useSelector } from "react-redux";
import { RootState } from "../../redux/redux.store";
import ReportsSavingsYearlyTable from "@components/PageReports/ReportsSavingsYearlyTable";

export default function SavingsYearlyTable() {
	// const { interest, save, withdraw } = useSelector((state: RootState) => state.savings.savingsUserTable);
	// return <ReportsSavingsYearlyTable save={save} interest={interest} withdraw={withdraw} />;
	return;
}
